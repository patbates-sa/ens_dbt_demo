name: 'ens_dbt_reference'
version: '1.0.0'
profile: 'ens_dbt_reference'

model-paths: ["models"]
snapshot-paths: ["snapshots"]
macro-paths: ["macros"]

models:
  ens_dbt_reference:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: view
      +schema: intermediate

    marts:
      +materialized: table
      +schema: marts

flags:
  require_generic_test_arguments_property: true